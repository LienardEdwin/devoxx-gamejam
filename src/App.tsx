import { PhaserGame } from './PhaserGame';

function App() {
    return (
        <div id="app" style={{ width: '100%', height: '100%', margin: 0, padding: 0, overflow: 'hidden', background: '#000' }}>
            <PhaserGame />
        </div>
    );
}

export default App;
